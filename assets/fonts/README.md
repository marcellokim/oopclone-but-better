# Bundled UI fonts

- Display: IBM Plex Serif Regular (`IBMPlexSerif-Regular.ttf`)
- Body: Lato Regular (`Lato-Regular.ttf`)
- Mono: IBM Plex Mono Regular (`IBMPlexMono-Regular.ttf`)

Each family is bundled from the upstream Google Fonts repository and keeps its matching OFL text beside the asset.
